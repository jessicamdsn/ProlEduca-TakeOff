{
  "buildCommand": "ng build --configuration=production --base-href ./",
  "outputDirectory": "dist/prol_educa_app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
